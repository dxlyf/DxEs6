{
  "compilerOptions": {
    /* Basic Options */
    "target": "es5",                         /*指定目标版本：'沙' ECMAScript（默认），“ES5 '，”es2015 '，' es2016 '，' es2017 '，或' esnext”。*/
    "module": "umd",                    /*指定模块代码生成：“CommonJS '，'和'，'系统'，“UMD”，“es2015 '，或' esnext”。*/
    "lib": ["es5","dom","es2015.promise"],                            /*指定要包含在编译中的库文件：*/
    // "allowJs": true,                      / *允许编译JavaScript文件。* /
    // "checkJs": true,                      / *报告js文件中的错误。* /
    // "jsx": "preserve",                    /*指定JSX代码生成：“保留”、“本土”的反应，或是“反应”。* /
    // "declaration": true,                  /*生成对应的d.ts”文件。* /
    // "sourceMap": true,                    /*生成相应的“映射”文件。*/
    // "outFile": "./",                      / *连接和发射输出到单个文件。* /
    // "outDir": "./",                       / *将输出结构重定向到目录。* /
    // "rootDir": "./",                      *指定输入文件的根目录。用以控制输出目录结构，outdir。* /
    // "removeComments": true,               / *不要向输出发出注释。* /
    // "noEmit": true,                       * *不发出输出。* /
    // "importHelpers": true,                / *进口发出助手从tslib”。* /
    // "downlevelIteration": true,           / *提供可迭代对象'为'，全力支持传播，和解构针对的ES5 '或'沙'。* /
    // "isolatedModules": true,              / * transpile每个文件作为一个单独的模块（类似于“ts.transpilemodule”）。* /

    /* Strict Type-Checking Options */
    "strict": true,                           /*启用所有严格的类型检查选项。*/
    "noImplicitAny": true,                /*在表达式和声明中加上隐含的“任何”类型错误。*/
    // "strictNullChecks": true,             / *启用严格的null检查。* /
    // "noImplicitThis": true,               / *在“这个”表达式上加上一个隐含的“任何”类型错误。* /
    // "alwaysStrict": true,                 *在严格模式下解析，并为每个源文件发出“严格使用”命令。* /

    /* Additional Checks */
    // "noUnusedLocals": true,               / *报告未使用的本地错误。* /
    // "noUnusedParameters": true,           / *报告未使用参数上的错误。* /
    // "noImplicitReturns": true,            / *当函数中的所有代码路径返回值时报告错误。* /
    // "noFallthroughCasesInSwitch": true,   / *在switch语句fallthrough例报告错误。* /

    /* Module Resolution Options */
    // "moduleResolution": "node",           /*指定模块的解决策略：'节点（节点。JS）或“经典”（打字pre-1.6）。* /
     "baseUrl": "./",                      /*用于解析非绝对模块名的基目录。*/
     "paths": {
     }                          /*一系列条目，再图进口查找位置相对的URL地址”。*/
    // "rootDirs": [],                       /*根文件夹的列表，其合并的内容代表项目在运行时的结构。* /
    // "typeRoots": [],                      /*要包含类型定义的文件夹列表。* /
    // "types": [],                          *在编译中包含的类型声明文件。* /
    // "allowSyntheticDefaultImports": true, /*允许默认导入模块，没有默认导出。这并不影响代码发出，只是类型检查。*/

    /* Source Map Options */
    // "sourceRoot": "./",                   /*指定位置的调试器应该定位打字文件而不是源位置。*/
    // "mapRoot": "./",                      *指定调试器应该定位映射文件而不是生成位置的位置。*/
    // "inlineSourceMap": true,              *发出源映射的单个文件，而不是单独文件。* /
    // "inlineSources": true,                /*发射源与sourcemaps在单个文件；要求-- inlinesourcemap '或'，'将sourcemap。*/

    /* Experimental Options */
    // "experimentalDecorators": true,       /*使实验支持的ES7装饰。* /
    // "emitDecoratorMetadata": true,        /*使实验支持发光型元数据的装饰。*/
  },
  "include": [
    "src_ts/**/*"
  ],
  "exclude": [
      "node_modules",
      "**/*.spec.ts"
  ]
}